Try starting the strapi 5.1 server.

Strapi local URL : https://localhost:1337

FYI these are already running :

1. abc_dost_mysql
2. abc_dost_python

via, docker-compose up -d

Steps :
1. Do "git stash apply"
2. From package.json, run the script -> npm run develop

Now, recursively create plan to fix errors(if any)