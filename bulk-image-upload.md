Can we create a JSON payload where whatever
fields which require image upload in the schema are mentioned.

Now, when I hit the payload on the API, it patches the API schema
with the provided images in those particular fields.

If this is possible, please create a roadmap of implementation.

Consider below API set :

investments-landing-pages
investments-dashboard-pages
investments-navigation
investments-journey-pages
investments-bank-detail
investments-bank-mapping
investments-csp-config

How big of a challenge is to implement this solution to the complete set of APIs listed above.